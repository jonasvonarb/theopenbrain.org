import { createStore } from "vuex";

export const store = createStore({
  // the data
  state() {
    return {};
  },
  // the synchronous code
  mutations: {},
  actions: {},
  getters: {},
  modules: {},
});
